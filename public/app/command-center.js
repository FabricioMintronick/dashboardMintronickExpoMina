import {overviewCards} from './overview-graphics.js';
import {fleetAnalytics} from './analytics.js';
import {trendHTML,mountTrend} from './visual.js';
import {escape as e,number,age} from './format.js';
const columns=[['rpm','Motor'],['coolant','Refrig.'],['hydtemp','Hidráulica'],['fuel','Tanque'],['voltage','Batería']];
export function commandCenter(page,{items}){
 const selected=items.find(i=>i.communication==='fresh'&&i.metrics.rpm.value!==null)||items[0];
 page.innerHTML=`<section class="command-hero"><div><span class="eyebrow">MINTRONICK / REMOTE OPERATIONS</span><h2>Tu operación.<br><em>En una sola mirada.</em></h2><p>Localiza la flota, identifica señales de atención y explora la evidencia.</p><div class="hero-actions"><a class="button" href="#alerts">Revisar alarmas ↗</a><a class="button secondary" href="#reports">Construir reporte</a></div></div><div class="hero-radar" aria-hidden="true"><div class="radar-ring"></div><img src="/tractor-d8-transparent.png" alt=""><span>TELEMETRÍA · GPS · HISTORIAL</span></div></section><div id="command-kpis">${overviewCards(items)}</div><div class="command-map-row"><section class="panel"><div class="panel-head"><div><span class="eyebrow">CONTEXTO GEOGRÁFICO</span><h2>¿Dónde está tu flota?</h2></div><a class="button secondary" href="#map">Abrir seguimiento ↗</a></div><div id="command-map"></div><div class="command-map-caption">Posiciones originales · Gris: ubicación atrasada · Selecciona un marcador</div></section><section class="panel attention-panel"><div class="panel-head"><div><span class="eyebrow">SIGUIENTE ACCIÓN</span><h2>Equipos para revisar</h2></div></div><div id="command-attention"></div></section></div><div id="command-analysis"></div><div class="command-data-row">${trendHTML(items,selected)}<section class="panel"><div class="panel-head"><div><h2>¿Qué datos tenemos?</h2><p>Una fila por equipo · Color = vigencia de cada señal</p></div></div><div id="command-matrix"></div><p class="chart-caption">Turquesa: reciente · Gris: atrasada · —: sin lectura. No representa salud mecánica.</p></section></div>`;
 const charts=fleetAnalytics(page.querySelector('#command-analysis'),items),trend=mountTrend(page,items);
 let map=null,markers=null;
 if(window.L){map=L.map('command-map',{scrollWheelZoom:false,zoomControl:false}).setView([-12,-77],5);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map);markers=L.layerGroup().addTo(map);}
 function update(next,initial=false){
 page.querySelector('#command-kpis').innerHTML=overviewCards(next);charts.update(next);trend.update(next);
 const priority=[...next].sort((a,b)=>b.alarms.filter(x=>x.quality==='fresh').length-a.alarms.filter(x=>x.quality==='fresh').length||Number(a.communication==='fresh')-Number(b.communication==='fresh')||a.name.localeCompare(b.name));
 page.querySelector('#command-attention').innerHTML=priority.slice(0,4).map(i=>`<button data-quick="${e(i.id)}" class="attention-row"><img src="/tractor-d8-transparent.png" alt=""><span><b>${e(i.name)}</b><small>${i.alarms.some(a=>a.quality==='fresh')?e(i.alarms.filter(a=>a.quality==='fresh').map(a=>a.label).join(' · ')):i.communication!=='fresh'?'Verificar comunicación':'Explorar telemetría'}</small></span><span>↗</span></button>`).join('')+'<p class="chart-caption">Primero: alarmas recientes. Después: sin comunicación reciente. Mostrando 4 equipos.</p>';
 page.querySelector('#command-matrix').innerHTML=`<table class="signal-matrix"><thead><tr><th>Equipo</th>${columns.map(([,label])=>`<th>${label}</th>`).join('')}</tr></thead><tbody>${next.map(i=>`<tr><td><button class="text-button" data-quick="${e(i.id)}">${e(i.name)}</button></td>${columns.map(([key])=>{const m=i.metrics[key];return `<td><a href="#history/${encodeURIComponent(i.id)}" class="matrix-cell ${m.value===null?'missing':m.quality==='fresh'?'fresh':'old'}" title="${e(m.label)} · ${number(m.value)} ${e(m.unit)} · ${e(age(m.at))}">${number(m.value)}</a></td>`;}).join('')}</tr>`).join('')}</tbody></table>`;
 if(map){markers.clearLayers();next.filter(i=>i.location).forEach(i=>L.circleMarker([i.location.lat,i.location.lon],{radius:7,color:i.location.quality==='fresh'?'#159c9c':'#7e8e8a',fillOpacity:.8,weight:2}).bindTooltip(e(i.name)).on('click',()=>{location.hash='#map/'+encodeURIComponent(i.id);}).addTo(markers));if(initial){const located=next.filter(i=>i.location);if(located.length)map.fitBounds(located.map(i=>[i.location.lat,i.location.lon]),{padding:[30,30],maxZoom:14});}}
 }
 update(items,true);const stop=()=>{charts();trend();map?.remove();};stop.update=update;return stop;
}
